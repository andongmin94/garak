export function InterfaceWorkspace() {
  return (
    <section className="workspace-card" aria-labelledby="interface-workspace-heading">
      <p className="placeholder-label">Phase 0B placeholder</p>
      <h2 id="interface-workspace-heading">Interface</h2>
      <p className="workspace-description">
        Plugin interface design will be implemented in a later phase.
      </p>
      <p className="scope-note">
        This view is only a workspace boundary; it is not a UI designer or preview surface.
      </p>
    </section>
  );
}
