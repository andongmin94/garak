export function ControlWorkspace() {
  return (
    <section className="workspace-card" aria-labelledby="control-workspace-heading">
      <p className="placeholder-label">Phase 0B placeholder</p>
      <h2 id="control-workspace-heading">Control</h2>
      <p className="workspace-description">
        Control mapping authoring will be implemented in a later phase.
      </p>
      <p className="scope-note">
        No editable mappings, automation behavior, or persistent control model exists yet.
      </p>
    </section>
  );
}
